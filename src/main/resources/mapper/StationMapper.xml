<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xz.fzu.model.StationLabel">
    <select id="getInstance" parameterType="int" resultType="java.lang.String">
        SELECT content FROM station_label WHERE station_id = #{stationId}
    </select>
</mapper>